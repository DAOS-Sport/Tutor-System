# 教練場館路由全域盤點（只讀版）

生成時間：2026-07-08

本輪依最新指令只建立本文件；未修改既有程式碼、未執行 `bash scripts/build-frontends.sh`、未重啟 Express。以下是針對「教練場館 / 授權場館」在後台、教練端、家長端的路由與前端呼叫鏈盤點。

## 0. Express Route Mount

來源：`server/index.js`

- `/api/auth` -> `server/routes/auth.js`
- `/api/venues` -> `server/routes/venues.js`
- `/api/coaches` -> `server/routes/coaches.js`
- `/api/coach-portal` -> `server/routes/coachPortal.js`
- `/api/slots` -> `server/routes/slots.js`
- `/api/enrollments` -> `server/routes/enrollments.js`
- `/api/admin` -> `server/routes/admin`

場館權威資料不是單一路由，而是由 `admin_staff` / `admin_staff_venues` / `coaches` / `coach_venues` / `ragic_h01_shadow` 共同串起來。現行設計方向是：F-A02 員工帳號管理為中心來源，`admin_staff_venues` 是主要多場館陣列，`admin_staff.venue_id` 是第一筆相容欄位，`coach_venues` 是教練表相容/下游查詢表。

## 1. 共用正規化入口

### 後端

檔案：`server/services/coachVenueScope.js`

- `cleanVenueText(value)`：字串化後 `.trim()`。
- `venueItems(values)`：支援 array、JSON array 字串、逗號/換行/頓號/分號字串。
- `cleanVenueList(values)`：去空、去重、保留順序。
- `COACH_VENUE_SOURCE_SQL`：教練場館來源 SQL。外層要求 alias `c=coaches`、`s=admin_staff`。
- `COACH_STAFF_PROFILE_SELECT`：教練對外 payload 欄位，姓名/手機/資深/倍率優先取 F-A02 `admin_staff`，並輸出 `venue_ids`。
- `STAFF_VENUE_IDS_SELECT`：F-A02 staff list 的多場館清單。
- `ADMIN_USER_VENUE_IDS_SELECT`：後台登入 JWT 的多場館清單。

重要觀察：

- `COACH_VENUE_SOURCE_SQL` 在有 `admin_staff s` 的情境下，主要用 `admin_staff_venues` + `admin_staff.venue_id`；`coach_venues` 只有 `s.id IS NULL` 時才進來。
- 這代表家長端與教練端 payload 目前被收斂到 F-A02 員工資料中心。

### LIFF 前端

檔案：`client/liff/src/utils/venues.js`

- `cleanVenueValue(value)`：取 string/number 或 object 的 `id/code/name` 後 `.trim()`。
- `venueItems(values)`：支援 array、JSON array 字串、逗號/換行/頓號/分號字串。
- `cleanVenueList(values)`：去空、去重。
- `coachVenueValues(coach)`：從 `coach.venue_ids || coach.venues` 取值。
- `coachMatchesVenue(coach, selectedValues)`：`some()` 比對，支援精確相等與 `includes()`。

## 2. 後台 Admin 路由與前端鏈

### Admin UI Route

檔案：`client/admin/src/App.jsx`

- `/staff` -> `StaffPage`，admin only。F-A02 員工帳號管理。
- `/coaches` -> `CoachesRedirect`。F-C-Admin 已合併進 F-A02。
- `/venues` -> `VenuesPage`，admin only。F-A03 場館設定。
- `/ragic-status` -> `RagicStatusPage`，admin only。
- `/ragic-staging` -> `RagicStagingPage`，admin only。
- `/reports` -> `ReportsPage`，admin/manager。

### Admin API Client

檔案：`client/admin/src/api/staff.js`

- `staffApi.list(params)` -> `GET /api/admin/staff`
- `staffApi.get(id)` -> `GET /api/admin/staff/:id`
- `staffApi.coachesByVenue(venueId, status)` -> `GET /api/admin/staff/coaches?venueId=&status=`
- `staffApi.create(body)` -> `POST /api/admin/staff`
- `staffApi.update(id, patch)` -> `PATCH /api/admin/staff/:id`
- `staffApi.syncRagic()` -> `POST /api/admin/staff/sync`

檔案：`client/admin/src/api/ragicStatus.js`

- `ragicStatusApi.get()` -> `GET /api/admin/ragic-status`
- `ragicStatusApi.sync(form)` -> `POST /api/admin/ragic-status/sync?form=...`
- `ragicStatusApi.toggle(job, enabled)` -> `POST /api/admin/ragic-status/toggle`

檔案：`client/admin/src/api/venues.js`

- `venuesApi.list()` -> `GET /api/admin/venues`
- `venuesApi.syncDryRun()` / `syncConfirm()` -> `POST /api/admin/venues/sync-ragic`
- `venuesApi.toggleActive()` -> `PATCH /api/admin/venues/:id/active`

### F-A02 Staff Page

檔案：`client/admin/src/pages/StaffPage.jsx`

- 載入場館字典：`venuesApi.list()`。
- 清單：`staffApi.list(normalizeFilters(filters))`。
- 場館 filter：若輸入 name，前端先轉成 venue id。
- 手動同步 H01：`syncRagic()` -> `staffApi.syncRagic()`，再輪詢 `ragicStatusApi.get()`。
- 新建 staff：送 `venue_ids` 與 `venue_id = venue_ids[0] || null`。
- 更新 staff：送 `venue_ids` 與 `venue_id = venue_ids[0] || null`。
- 表格場館欄：顯示 `venue_ids` chips，fallback `venue_id`。
- 表格教練資料欄：顯示 `has_coach_profile` / `coach_active`，可切換上架。

檔案：`client/admin/src/pages/StaffEditModal.jsx`

- `VenueChipsField` 是 F-A02 的場館多選 UI。
- Ragic 來源員工 `ragic_locked` 時，姓名/手機/場館前端唯讀。
- 非 coach 角色可勾 `啟用教練 LIFF 身分（兼任教練）`，但場館仍取同一份 `venue_ids`。

### 後台手動建檔與報名編輯

檔案：`client/admin/src/pages/ManualEnrollPage.jsx`

- 場館下拉來自 `venuesApi.list()`。
- 選場館後：`staffApi.coachesByVenue(venueId, 'active')`。
- 選教練下拉只吃該 API 回來的教練清單。

檔案：`client/admin/src/pages/enrollments/EditEnrollmentModal.jsx`

- 場館下拉來自 `venuesApi.list()`。
- 選場館後：`staffApi.coachesByVenue(venueId, 'active')`。
- 變更場館時會 reset `coachId`，要求重新選教練。

### Admin Backend Staff Routes

檔案：`server/routes/admin/staff.js`

- `GET /api/admin/staff`
  - 使用 `STAFF_SELECT` + `STAFF_VENUE_IDS_SELECT`。
  - `rowToStaff()` 將 `r.venue_ids` 經 `cleanVenueList()` 輸出。
  - `venueId` filter 目前查 `s.venue_id OR admin_staff_venues`。

- `POST /api/admin/staff/sync`
  - 立刻回 202。
  - 背景跑 `syncStaffFromRagic('manual')`。
  - 重複觸發由 `isJobRunning('staff')` / single-flight 合併。

- `GET /api/admin/staff/coaches`
  - 給手動建檔、報名編輯的教練下拉。
  - 使用 `COACH_STAFF_PROFILE_SELECT` 回 `venue_ids`。
  - `venueId` filter 目前同時查 `coach_venues`、`admin_staff_venues`、`s.venue_id`。

- `GET /api/admin/staff/:id`
  - staff detail，含 coach profile / bio media。

- `POST /api/admin/staff`
  - 接 `venue_ids`，寫 `admin_staff.venue_id` 第一筆。
  - `syncStaffVenues()` 寫 `admin_staff_venues`。
  - role=coach 時 `ensureCoachRow()`，再 `syncCoachVenues()`。

- `PATCH /api/admin/staff/:id`
  - Ragic locked 時忽略姓名/手機/場館 patch。
  - 非 locked 時 `pickVenueIds()` -> `syncStaffVenues()`。
  - role=coach 或兼任教練時更新/建立 `coaches`。

### Admin Login / Scope / WebSocket

檔案：`server/routes/admin/auth.js`

- `POST /api/admin/auth/login`
  - 使用 `ADMIN_USER_VENUE_IDS_SELECT`。
  - `_effectiveLoginUser()` 會把掛在 F-A02 的 admin/manager/staff 角色提升到登入角色。
  - JWT payload 含 `venue_id` 與 `venue_ids`。

檔案：`server/middlewares/adminAuth.js`

- `requireAdminAuth()` 會把 token 內 `venue_ids` 經 `cleanVenueList()`。
- `getScopedVenueIds()`：admin 回 `null` 代表全部；manager/staff 回自己的 `venue_ids`，空值 fail closed。
- `isVenueInScope()`：用 `venue_ids` 判斷單筆場館權限。

檔案：`server/services/websocket.js`

- `/ws/admin` 解析 JWT 後保留 `adminVenueIds`。
- `broadcastAdminEvent()` 對非 admin 以 `payload.venue_id in adminVenueIds` 過濾。

### Admin Reports 旁支

檔案：`server/routes/admin/reports.js`

- `GET /api/admin/reports/coach-options`
  - 僅回 `{ id, name }`，目前不回 `venue_ids`。
  - 報表頁下拉用，不應被視為授權場館來源。

## 3. Ragic H01 / Shadow / Reconcile 鏈

檔案：`server/services/ragicAdmin.js`

### 場館解析

- `_extractStaffVenueIds(r)`
  - 從 H01 record 抽場館。
  - 支援 env `RAGIC_FIELD_H01_VENUE_PRIMARY` / `RAGIC_FIELD_H01_VENUE_SUPPORT`。
  - 支援中文欄位：主場館、主要場館、支援場館、服務場館、場館、部門。
  - 以逗號、頓號、空白拆分，再 `.trim()`。

- `_buildVenueResolver()`
  - 從 `venues` 建立 name/full_name/id -> venue id map。
  - 也處理 `三重商工 (test)` 這類括號後綴。

- `_staffPayloadFromRagicRow(r, resolveVenues)`
  - 輸出 `venue_ids: resolveVenues(_extractStaffVenueIds(r))`。
  - 同時輸出 `is_coach` / `is_counter` / `is_lifeguard`。

### 寫正式表

- `_syncCoachVenueIds(client, coachId, rawVenueIds)`
  - resolve 成 venue id。
  - 查 `venues WHERE is_active = TRUE`。
  - 刪除並重建 `coach_venues`。

- `_syncStaffVenueIds(client, staffId, rawVenueIds)`
  - resolve 成 venue id。
  - 查 `admin_venues`。
  - 刪除並重建 `admin_staff_venues`。
  - 同步 `admin_staff.venue_id` 第一筆。

- `_applyStaffChange(row, client)`
  - 建立/更新 `admin_staff`。
  - role=coach 或 H01 `is_coach` 命中時建立/更新 `coaches`。
  - 教練分支會 `_syncCoachVenueIds(... p.venue_ids)`。
  - 最後 `_syncStaffVenueIds(... p.venue_ids)`。

### 同步入口

- `kickoffSyncStaffAsync()`：清單載入時背景觸發。
- `syncStaffFromRagic(triggeredBy)`：所有 staff sync 最終進 `_singleflight('staff')`。
- `_runWithLog()`：寫 `ragic_sync_log`。
- `RAGIC_LOCKED_JOBS`：`staff`, `venues`, `parents`, `students`, `backup`, `pull` 共用 `ragic_sync` lock。
- `getSyncStatusSnapshot()`：給 `/api/admin/ragic-status` 顯示 `admin_enabled`、`in_progress`、watermark、last status。

### Ragic Status / Staging Routes

檔案：`server/routes/admin/ragicStatus.js`

- `GET /api/admin/ragic-status`
- `POST /api/admin/ragic-status/sync?form=staff|venues|parents|students|backup|pull|all`
  - 先檢查 Ragic env flags。
  - 立刻 202，背景執行。

檔案：`server/routes/admin/ragicStaging.js`

- `GET /api/admin/ragic-staging`
- `POST /api/admin/ragic-staging/:id/approve`
- `POST /api/admin/ragic-staging/:id/merge`
- `POST /api/admin/ragic-staging/:id/reject`
- `POST /api/admin/ragic-staging/bulk-approve`

## 4. 教練端 LIFF 路由與前端鏈

### LIFF Route

檔案：`client/liff/src/App.jsx`

- `/coach-portal` -> `CoachPortalLoginPage`
- `/coach` -> `CoachTodayPage`
- `/coach/schedule` -> `CoachScheduleWeekPage`
- `/coach/profile` -> `CoachProfilePage`
- `/coach/session/:id` -> `CoachSessionPage`

### Coach API Client

檔案：`client/liff/src/api/coachPortal.js`

- `status()` -> `GET /api/coach-portal/auth/line/status`
- `exchange(code)` -> `POST /api/coach-portal/auth/exchange`
- `linkByName(token, name)` -> `POST /api/coach-portal/link-by-name`
- `resume(portalToken)` -> `GET /api/coach-portal/session`
- `logout(portalToken)` -> `POST /api/coach-portal/logout`

檔案：`client/liff/src/api/coaches.js`

- `coachesApi.byPhone()` -> `GET /api/coaches/by-phone`
- `coachesApi.byLineUid()` -> `GET /api/coaches/by-line-uid`
- `coachesApi.detail(id)` -> `GET /api/coaches/:id`

檔案：`client/liff/src/api/slots.js`

- `slotsApi.listByCoach()` -> `GET /api/slots/coach/:coachId`
- `slotsApi.create()` -> `POST /api/slots`
- `slotsApi.batch()` -> `POST /api/slots/batch`
- `slotsApi.previewConflict()` -> `POST /api/slots/preview-conflict`

### Coach Portal Backend

檔案：`server/routes/coachPortal.js`

- `loadCoach(coachId)` 使用 `COACH_STAFF_PROFILE_SELECT`。
- `issueLogin(coach)` 回傳 `coach.venue_ids` / `coach.venues`。
- `POST /api/coach-portal/auth/exchange`
  - handoff code -> `loadCoach()` -> `issueLogin()`。
- `POST /api/coach-portal/link-by-name`
  - 姓名 fallback 綁定 -> `loadCoach()` -> `issueLogin()`。
- `GET /api/coach-portal/session`
  - 30 天 portal token -> `loadCoach()` -> 新 JWT。

### Legacy Coach Login / Profile Backend

檔案：`server/routes/coaches.js`

- `GET /api/coaches/by-phone`
  - 以 F-A02 `s.phone` 查 active staff + active coach。
  - 使用 `COACH_STAFF_PROFILE_SELECT`。

- `GET /api/coaches/by-line-uid`
  - 查 `(c.line_uid = $1 OR s.line_uid = $1)`。
  - 使用 `COACH_STAFF_PROFILE_SELECT`。

- `GET /api/coaches/:id`
  - 教練端排課頁重新抓最新 `venue_ids` 的主要路由。
  - `loadCoach()` 只回 active staff + active coach。

檔案：`server/routes/auth.js`

- `POST /api/auth/demo-login`
  - demo coach 也使用 `COACH_STAFF_PROFILE_SELECT`。

### Coach Schedule Frontend

檔案：`client/liff/src/context/AuthContext.jsx`

- `setCoach()` 寫入 localStorage 前會用 `cleanVenueList()` 正規化 `venue_ids/venues`。

檔案：`client/liff/src/pages/CoachScheduleWeekPage.jsx`

- 初始 coach 來自 AuthContext。
- 掛載後再 `coachesApi.detail(coach.id)` 取得最新 `venue_ids`。
- `venuesApi.list()` 只作為 venue id -> name 顯示字典。
- header filter chips 走 `venueIds.map(...)`。
- 新增單筆與批量檔位都把 `venueIds` 傳入彈窗。

檔案：`client/liff/src/components/coach/AddSlotModal.jsx`

- `cleanVenueIds = cleanVenueList(venueIds)`。
- 場館 `<select>` 只渲染 `cleanVenueIds`。
- `slotsApi.create({ coach_id, venue_id, ... })`。

檔案：`client/liff/src/components/coach/BatchAddSlotModal.jsx`

- 同上，但送 `slotsApi.batch({ coach_id, venue_id, ... })`。

### Coach Slots Backend

檔案：`server/routes/slots.js`

- `GET /api/slots/coach/:coachId`
  - `requireCoach` + `requireCoachOwner('coachId')`。
  - 回 slots，本身不提供授權場館清單。

- `POST /api/slots`
  - `requireCoach` + `ensureBodyOwner`。
  - 目前看到的是驗證 coach owner，未看到再以 F-A02/coach venue 陣列驗證 body `venue_id`。

- `POST /api/slots/batch`
  - 同上。

- `POST /api/slots/preview-conflict`
  - 檢查時段衝突，不處理場館授權。

## 5. 家長端 LIFF 路由與前端鏈

### LIFF Route

檔案：`client/liff/src/App.jsx`

- `/venue` -> `VenueSelectPage`
- `/coaches` -> `CoachListPage`
- `/enroll` -> `EnrollmentPage`

### Parent Coach Filter Frontend

檔案：`client/liff/src/pages/CoachListPage.jsx`

- 讀 query `venue` 與 `courseType`。
- 同時呼叫：
  - `coachesApi.list({ venueId })` -> `GET /api/coaches?venueId=...`
  - `venuesApi.detail(venueId)` -> `GET /api/venues/:id`
  - `coursesApi.basePrice(courseType)`
- 前端再防禦 filter：
  - `coachMatchesVenue(c, [venueId, venue?.id, venue?.name])`
  - level filter
  - name query

### Parent Coach Backend

檔案：`server/routes/coaches.js`

- `GET /api/coaches?venueId=...`
  - `resolveVenueFilterCandidates(req.query.venueId)` 把名稱/代碼/full_name 轉成候選 venue ids。
  - SQL 使用 `EXISTS (SELECT 1 FROM (${COACH_VENUE_SOURCE_SQL}) match_venues WHERE venue_id = ANY(...))`。
  - 使用 `COACH_STAFF_PROFILE_SELECT`。
  - `publicCoach()` 移除 `line_uid` / staff governance 欄位。

- `GET /api/coaches/:id`
  - 報名頁 boot / 教練詳情也會用到。

### Parent Enrollment Downstream

檔案：`client/liff/src/pages/EnrollmentPage.jsx`

- query 取 `venue`、`courseType`、`coach`。
- `useEnrollmentBoot()` 取教練與場館。
- submit payload 送：
  - `coach: { id, name, is_senior }`
  - `venue: { id, name }`
  - `course_type`

檔案：`server/routes/enrollments.js`

- `POST /api/enrollments`
  - 驗證 coach UUID 且 `coaches.is_active = TRUE`。
  - 驗證 venue 存在且 active。
  - 目前未看到「coach 必須屬於該 venue」的 server-side 關聯檢查。
  - 寫入 `admin_enrollments.coach_id` 與 `venue_id`。

## 6. 場館字典路由

檔案：`server/routes/venues.js`

- `GET /api/venues`
  - 家長/LIFF 端場館列表，只回 `venues.is_active = TRUE`。
- `GET /api/venues/:id`
  - 家長/LIFF 端單一場館，只回 active venue。

檔案：`server/routes/admin/venues.js`

- `GET /api/admin/venues`
  - 後台 F-A03 場館設定，讀 `admin_venues`。
- `POST /api/admin/venues/sync`
  - fire-and-forget H05 同步。
- `POST /api/admin/venues/sync-ragic`
  - dry-run / confirm 兩階段。
- `PATCH /api/admin/venues/:id/active`
  - 啟用/停用場館。

## 7. 盤點到的脫鉤/風險點

1. `GET /api/admin/staff/coaches` 的 filter 仍同時看 `coach_venues`、`admin_staff_venues`、`s.venue_id`。
   - 但回傳 payload 的 `venue_ids` 來自 `COACH_STAFF_PROFILE_SELECT`，在有 `admin_staff` 時偏向 F-A02。
   - 若 `coach_venues` 有舊資料而 `admin_staff_venues` 沒有，可能出現「filter 撈得到，但 payload 不含該館」的不一致。

2. `POST /api/slots` / `POST /api/slots/batch` 目前只看到 owner check，未看到 `venue_id` 必須屬於該教練授權場館的 server-side check。
   - UI 已限制下拉，但 API 層仍應補硬規則。

3. `POST /api/enrollments` 目前只分別驗證 active coach 與 active venue，未看到 coach-venue 關聯檢查。
   - 家長端 `/coaches` 頁會先篩，但 submit API 本身仍可能被直接帶入 mismatch payload。

4. `POST /api/admin/enrollments` 手動建檔路由若帶 `coach_id`，目前只查 coach id 是否存在，未看到建立當下驗證 coach 屬於 `venueId`。
   - Admin 編輯報名 `PATCH /api/admin/enrollments/:id` 有檢查 `coach_venues`。

5. `_syncCoachVenueIds()` / `_syncStaffVenueIds()` 在解析不到任何 venue id 時直接 return，未看到清空既有 join rows。
   - 這可能是保護 Ragic 髒資料，但也代表 Ragic H01 場館被清空/改壞時，本地舊場館不會被清掉。

6. `GET /api/admin/reports/coach-options` 只回 active coaches id/name，不含場館，也不 scoped by venue。
   - 報表下拉用途可接受，但不能被當成授權來源。

7. Ragic 手動同步「看起來未同步」常見原因不在前端按鈕，而在 switches：
   - Ragic env 不完整。
   - `ragic_sync_enabled_staff` 被關閉。
   - 另一個 Ragic locked job 持有 `ragic_sync` lock。
   - single-flight 已有同 job in progress。
   - freshness/canary/watermark 狀態造成 last_status 為 skipped/stale_read/error。

## 8. Switch / Env 清單

### Build / Runtime

- `PORT`
- `NODE_ENV`
- `JWT_SECRET`
- `JWT_EXPIRES_IN`
- `ALLOW_DEMO_LOGIN`
- `REQUIRE_LINE_ID_TOKEN`
- `LIFF_URL`
- `LIFF_URL_PARENT`
- `LIFF_URL_COACH`
- `VITE_USE_MOCK`
- `VITE_LIFF_ID`
- `VITE_LIFF_ID_PARENT`
- `VITE_LIFF_ID_COACH`
- `VITE_SUPPORT_LINE_COACH`
- `VITE_SUPPORT_LINE_PARENT`
- `SMOKE_ADMIN_USER`
- `SMOKE_ADMIN_PASS`
- `SMOKE_BASE`
- `SKIP_SMOKE`

### Ragic Form / Field / API

- `RAGIC_API_KEY`
- `RAGIC_ACCOUNT`
- `RAGIC_BASE_URL`
- `RAGIC_FORM_H01`
- `RAGIC_FORM_H23`
- `RAGIC_FORM_H05`
- `RAGIC_FORM_Z01`
- `RAGIC_FORM_Z02`
- `RAGIC_FIELD_H01_VENUE_PRIMARY`
- `RAGIC_FIELD_H01_VENUE_SUPPORT`
- `RAGIC_FIELD_H01_LINE_UID`
- `RAGIC_FIELD_Z01_LINE_UID`
- `RAGIC_FIELD_H01_400LINE_MESSAGE`
- `RAGIC_H01_BLOCKED_FIELD_IDS`
- `RAGIC_CACHE_TTL_MS`
- `RAGIC_IGNORE_FIXED_FILTER`
- `RAGIC_TIMEOUT_MS`
- `RAGIC_API_VERSION`

### Freshness / Canary / Lock

- `RAGIC_CANARY_NONCE_FIELD_ID`
- `RAGIC_CANARY_FIELD_ID`
- `RAGIC_CANARY_NONCE_FIELD_NAME`
- `RAGIC_CANARY_IDENTIFIER_FIELD_ID`
- `RAGIC_CANARY_IDENT_FIELD_ID`
- `RAGIC_CANARY_IDENTIFIER_FIELD_NAME`
- `RAGIC_CANARY_IDENT_FIELD_NAME`
- `RAGIC_CANARY_IDENTIFIER_VALUE`
- `RAGIC_CANARY_IDENT_VALUE`
- `RAGIC_CANARY_H01_RECORD_ID`
- `RAGIC_CANARY_H05_RECORD_ID`
- `RAGIC_CANARY_Z01_RECORD_ID`
- `RAGIC_CANARY_Z02_RECORD_ID`
- `RAGIC_FRESHNESS_RETRIES`
- `RAGIC_FRESHNESS_BACKOFF_MS`
- `RAGIC_FRESHNESS_REQUIRE_CANARY`
- `RAGIC_MANUAL_LOCK_WAIT_MS`
- `RAGIC_MANUAL_LOCK_POLL_MS`
- `RAGIC_STATUS_PROBE_TTL_MS`

### DB Stored Switches

來源：`server/services/ragicAdmin.js`

- `admin_settings.key = ragic_sync_enabled_<job>`
  - jobs：`staff`, `venues`, `parents`, `students`, `backup`, `pull`, `quarantine`
- `admin_settings.key = ragic_watermark_<formCode>`
  - 用來判斷下一次手動同步 full/incremental。

## 9. 凍結測試建議（未建立，因本輪禁止改程式碼）

建議新增 `tests/venue_flow_consistency_test.js`，測以下斷言：

1. `GET /api/coaches?venueId=三重商工`
   - 必須包含「蔣碩仁」。
   - `venue_ids` 必須包含 `新北高中/三重商工/三民高中` 對應代碼，或至少包含 `B/K/L`。

2. `GET /api/coaches/:id`
   - 同一 coach id 必須回完整 `venue_ids`。
   - 不可只剩第一筆。

3. `GET /api/coach-portal/session`
   - 教練 session resume 後 payload 必須回完整 `venue_ids`。

4. `GET /api/admin/staff/coaches?venueId=K`
   - 必須包含該教練。
   - 回傳 `venue_ids` 必須仍是完整陣列。

5. `GET /api/admin/staff?venueId=K&role=coach`
   - F-A02 清單必須包含該教練/兼任教練。

6. `POST /api/slots` / `POST /api/slots/batch`
   - authorized venue 成功。
   - unauthorized venue 應 403/400。

7. `POST /api/enrollments`
   - coach 屬於 venue 成功。
   - coach 不屬於 venue 應 400。

8. `POST /api/admin/enrollments`
   - 帶 `coach_id` 時必須驗證 coach 屬於 `venue_id`。

## 10. 本次只讀盤點命令

本次使用 `rg` / `nl -ba` 檢視以下重點：

- `server/index.js`
- `server/routes/coaches.js`
- `server/routes/coachPortal.js`
- `server/routes/slots.js`
- `server/routes/enrollments.js`
- `server/routes/admin/staff.js`
- `server/routes/admin/auth.js`
- `server/routes/admin/enrollments.js`
- `server/routes/admin/ragicStatus.js`
- `server/routes/admin/ragicStaging.js`
- `server/routes/admin/venues.js`
- `server/routes/admin/reports.js`
- `server/middlewares/adminAuth.js`
- `server/services/coachVenueScope.js`
- `server/services/ragicAdmin.js`
- `server/services/websocket.js`
- `client/admin/src/App.jsx`
- `client/admin/src/api/staff.js`
- `client/admin/src/api/ragicStatus.js`
- `client/admin/src/api/venues.js`
- `client/admin/src/pages/StaffPage.jsx`
- `client/admin/src/pages/StaffEditModal.jsx`
- `client/admin/src/pages/ManualEnrollPage.jsx`
- `client/admin/src/pages/enrollments/EditEnrollmentModal.jsx`
- `client/admin/src/pages/ReportsPage.jsx`
- `client/liff/src/App.jsx`
- `client/liff/src/api/coaches.js`
- `client/liff/src/api/coachPortal.js`
- `client/liff/src/api/slots.js`
- `client/liff/src/api/venues.js`
- `client/liff/src/context/AuthContext.jsx`
- `client/liff/src/pages/CoachListPage.jsx`
- `client/liff/src/pages/CoachScheduleWeekPage.jsx`
- `client/liff/src/components/coach/AddSlotModal.jsx`
- `client/liff/src/components/coach/BatchAddSlotModal.jsx`
- `client/liff/src/pages/EnrollmentPage.jsx`
- `client/liff/src/utils/venues.js`

