node_modules/
.DS_Store
*.log
.env
.env.local
dist/
build/
coverage/
.vscode/
.idea/

# runtime upload artifacts (driver: local)
server/uploads/

# build 版本戳記（由 scripts/build-frontends.sh 產生，不進版控避免過時）
server/build-info.json

# codebase-memory-mcp — knowledge-graph MCP tool (dev-only, not part of the app)
# 1.3GB C/C++ source tree + machine-specific MCP config; keep out of the published Replit app
/codebase-memory-mcp/
.mcp.json
.cbmignore

# agent runtime — binaries (254MB MCP binary) and session state should not be in git.
# Use "git rm --cached -r .local/bin/ .local/state/" to remove already-tracked copies.
.local/bin/
.local/state/

# Phase 0 診斷輸出與資料清理備份：含正式資料快照，不入版控
reports/
